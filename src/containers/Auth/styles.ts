import { createStyles } from '@material-ui/core';

const styles = createStyles({
    fill: {
        position: 'relative',
        overflowX: 'hidden'
    },
    footer: {
        position: 'fixed',
        bottom: 0,
        width: '100%',
        height: 55
    }
});

export default styles;
