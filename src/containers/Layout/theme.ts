import { createMuiTheme } from '@material-ui/core';
const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#0277bd',
            contrastText: '#fff',
        },
        secondary: {
            main: '#35c191',
            contrastText: '#fff',
        }
    },
});

export default theme;
